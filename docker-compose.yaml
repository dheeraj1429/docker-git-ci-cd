version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    container_name: server
    environment:
      - PORT=3000
    ports:
      - 3000:3000